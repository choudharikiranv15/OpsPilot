# Production dependencies
# Install with: pip install -r requirements.txt
# Or better: pip install -e .

typer>=0.12.0,<1.0
rich>=13.7.0,<14.0
httpx>=0.27.0,<1.0
python-dotenv>=1.0.0,<2.0
pydantic>=2.6.0,<3.0
pydantic-settings>=2.2.0,<3.0
structlog>=24.1.0,<25.0

# Optional dependencies (install with pip install opspilot-ai[redis,aws,k8s])
# redis>=5.0.0,<6.0
# boto3>=1.34.0,<2.0
# kubernetes>=29.0.0,<30.0
